[phases.setup]
nixPkgs = ['ffmpeg', 'tesseract', 'tesseract.traineddata.eng', 'tesseract.traineddata.rus']

[phases.install]
cmds = [
  'apt-get update',
  'apt-get install -y tesseract-ocr tesseract-ocr-rus',
  'pip install -r requirements.txt'
]
